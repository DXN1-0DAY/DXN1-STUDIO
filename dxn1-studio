#!/usr/bin/env python3
"""DXN1 STUDIO — entry point. Launches the IDE from the dxn1_studio package."""

import os
import sys

sys.path.insert(0, os.path.dirname(os.path.realpath(__file__)))

from dxn1_studio.app import main  # noqa: E402

if __name__ == "__main__":
    main()
